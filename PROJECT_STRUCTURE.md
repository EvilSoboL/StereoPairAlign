# 📁 Структура проекта

Полная структура файлов системы совмещения изображений.

## Основная структура

```
image-alignment/
│
├── 📄 Основные модули (обязательные)
│   ├── image_alignment.py          # Ядро алгоритма совмещения
│   ├── homography_decomposer.py    # Декомпозиция гомографии
│   ├── visualizer.py               # Создание визуализаций
│   ├── batch_processor.py          # Пакетная обработка
│   └── __init__.py                 # Инициализация пакета
│
├── 🚀 CLI и утилиты
│   ├── main.py                     # Главный CLI интерфейс
│   ├── test_single_pair.py         # Тестирование одной пары
│   └── example_usage.py            # Примеры использования API
│
├── 🧪 Тестирование
│   └── test_alignment.py           # Unit и интеграционные тесты
│
├── 📚 Документация
│   ├── README.md                   # Полная документация
│   ├── QUICKSTART.md               # Быстрый старт
│   ├── PROJECT_STRUCTURE.md        # Этот файл
│   └── LICENSE                     # MIT лицензия
│
├── ⚙️ Конфигурация
│   ├── requirements.txt            # Python зависимости
│   ├── setup.py                    # Установка как пакет
│   ├── config.yaml                 # Опциональная конфигурация
│   └── .gitignore                  # Git игнорирование
│
├── 📂 Рабочие директории (создаются автоматически)
│   ├── images/                     # Входные BMP изображения
│   │   ├── image7_b.bmp           # Эталон (камера 1)
│   │   ├── image8_b.bmp           # Для совмещения (камера 2)
│   │   └── ...
│   │
│   └── results/                    # Выходные результаты
│       ├── image8_b_transform.json
│       ├── summary_report.json
│       └── visualizations/
│           ├── image8_b_heatmap.jpg
│           ├── image8_b_overlay.jpg
│           └── ...
│
└── 📝 Дополнительные файлы
    ├── processing.log              # Лог-файл (если включен)
    └── .venv/                      # Виртуальное окружение
```

## 📄 Описание основных модулей

### image_alignment.py
**Размер:** ~300 строк  
**Класс:** `ImageAligner`

Основной модуль для совмещения изображений:
- Детекция ключевых точек (ORB/SIFT/AKAZE)
- Сопоставление особенностей (Feature matching)
- Вычисление гомографии через RANSAC
- Применение преобразования
- Оценка качества (RMSE, inlier ratio)

**Ключевые методы:**
```python
aligner = ImageAligner(feature_detector='orb')
results = aligner.align_images(ref_img, target_img)
aligned = aligner.apply_homography(img, H, shape)
```

### homography_decomposer.py
**Размер:** ~200 строк  
**Класс:** `HomographyDecomposer`

Декомпозиция матрицы гомографии:
- SVD разложение для извлечения rotation/scale
- Извлечение translation из 3-го столбца
- Анализ perspective компонент
- Восстановление матрицы из компонент

**Ключевые методы:**
```python
decomposer = HomographyDecomposer()
components = decomposer.decompose(H)
H_reconstructed = decomposer.reconstruct_from_components(...)
```

### visualizer.py
**Размер:** ~250 строк  
**Класс:** `AlignmentVisualizer`

Создание визуализаций для проверки качества:
- Before/After сравнение
- Тепловая карта разницы (heatmap)
- Наложение с прозрачностью (overlay)
- Шахматное сравнение (checkerboard)
- Отображение matches

**Ключевые методы:**
```python
viz = AlignmentVisualizer()
heatmap = viz.create_difference_heatmap(ref, aligned)
overlay = viz.create_overlay(ref, aligned)
checker = viz.create_checkerboard(ref, aligned)
```

### batch_processor.py
**Размер:** ~350 строк  
**Класс:** `BatchProcessor`

Пакетная обработка множества пар:
- Автоматический поиск пар (нечетное-четное)
- Обработка всех пар в директории
- Создание JSON отчетов
- Сохранение визуализаций
- Сводная статистика

**Ключевые методы:**
```python
processor = BatchProcessor(input_dir, output_dir)
results = processor.process_all()
```

## 🚀 CLI интерфейсы

### main.py
**Размер:** ~250 строк  
**Функция:** `main()`

Главный командный интерфейс:
```bash
python main.py --input ./images --output ./results
python main.py -i ./images -o ./results --detector sift --viz
```

**Параметры:**
- `-i, --input` - входная директория
- `-o, --output` - выходная директория
- `--detector` - тип детектора (orb/sift/akaze)
- `--viz` - создавать визуализации
- `-v` - детальное логирование
- `--log` - файл лога

### test_single_pair.py
**Размер:** ~200 строк  
**Функция:** `main()`

Быстрое тестирование одной пары:
```bash
python test_single_pair.py ref.bmp target.bmp --show
python test_single_pair.py ref.bmp target.bmp --save ./test_results
```

### example_usage.py
**Размер:** ~350 строк  
**8 примеров использования**

Демонстрация программного API:
```python
from example_usage import *
example_1_basic_alignment()
example_2_decompose_homography()
```

## 🧪 Тестирование

### test_alignment.py
**Размер:** ~400 строк  
**Framework:** pytest

Unit и интеграционные тесты:
- `TestHomographyDecomposer` - тесты декомпозиции
- `TestImageAligner` - тесты совмещения
- `TestVisualizer` - тесты визуализаций
- `TestIntegration` - полный pipeline

**Запуск:**
```bash
pytest test_alignment.py -v
pytest test_alignment.py -v --cov
```

## 📦 Установка и зависимости

### requirements.txt
Основные зависимости:
- `opencv-python>=4.8.0`
- `opencv-contrib-python>=4.8.0`
- `numpy>=1.24.0`
- `Pillow>=10.0.0`

### setup.py
Установка как Python пакет:
```bash
pip install -e .  # Установка в режиме разработки
pip install .     # Обычная установка
```

После установки доступны команды:
```bash
align-images --input ./images --output ./results
test-alignment ref.bmp target.bmp
```

## 📊 Выходные форматы

### JSON Transform File
**Имя:** `image<N>_b_transform.json`
```json
{
  "reference_file": "image7_b.bmp",
  "target_file": "image8_b.bmp",
  "processing_time_sec": 2.145,
  "homography": [[...], [...], [...]],
  "transform_decomposed": {...},
  "quality": {...}
}
```

### Summary Report
**Имя:** `summary_report.json`
```json
{
  "total_pairs": 10,
  "successful": 10,
  "average_rmse": 0.847,
  "average_inlier_ratio": 0.89,
  "pairs": [...]
}
```

### Visualizations
**Форматы:** JPG
- `*_before_after.jpg` - Сравнение до/после
- `*_heatmap.jpg` - Тепловая карта разницы
- `*_overlay.jpg` - Наложение с прозрачностью
- `*_matches.jpg` - Совпадающие точки
- `*_checkerboard.jpg` - Шахматное сравнение

## 🔄 Зависимости между модулями

```
main.py
  └── batch_processor.py
       ├── image_alignment.py
       ├── homography_decomposer.py
       └── visualizer.py

test_single_pair.py
  ├── image_alignment.py
  ├── homography_decomposer.py
  └── visualizer.py

test_alignment.py (tests)
  ├── image_alignment.py
  ├── homography_decomposer.py
  └── visualizer.py
```

**Важно:** Все 4 основных модуля должны находиться в одной директории!

## 🎯 Минимальная установка

Для базовой работы необходимы только:

```
image-alignment/
├── image_alignment.py          # Обязательно
├── homography_decomposer.py    # Обязательно
├── visualizer.py               # Обязательно
├── batch_processor.py          # Обязательно
├── main.py                     # Для CLI
└── requirements.txt            # Зависимости
```

```bash
pip install -r requirements.txt
python main.py -i ./images -o ./results
```

## 📖 Порядок изучения

1. **Быстрый старт:** `QUICKSTART.md`
2. **Понимание алгоритма:** `README.md` → Раздел "Алгоритм работы"
3. **Запуск:** `main.py --help`
4. **Тестирование:** `test_single_pair.py`
5. **Программное использование:** `example_usage.py`
6. **Настройка:** `config.yaml` и параметры CLI
7. **Тестирование:** `test_alignment.py`

## 🔧 Режимы использования

### 1. CLI (командная строка)
```bash
python main.py -i ./images -o ./results
```

### 2. Python скрипт

```python
from batch_processor import BatchProcessor

processor = BatchProcessor('data/images', './results')
results = processor.process_all()
```

### 3. Установленный пакет
```bash
pip install -e .
align-images -i ./images -o ./results
```

### 4. Библиотека
```python
from image_alignment import ImageAligner
aligner = ImageAligner()
results = aligner.align_images(ref_img, target_img)
```

## 📝 Логи и отладка

### Лог-файлы
- **Консоль:** Стандартный вывод (stdout)
- **Файл:** `--log processing.log` (опционально)

### Уровни логирования
- **INFO:** Основная информация (по умолчанию)
- **DEBUG:** Детальная информация (`-v`)
- **WARNING:** Предупреждения
- **ERROR:** Ошибки

### Пример лога
```
2025-01-15 14:23:45 - INFO - Найдена пара: image7_b.bmp <-> image8_b.bmp
2025-01-15 14:23:46 - INFO - Найдено 4832 ключевых точек
2025-01-15 14:23:46 - INFO - Inliers: 1987/2156 (92.16%)
2025-01-15 14:23:46 - INFO - RMSE: 0.734 пикселей
```

## 🎓 Расширение функциональности

### Добавление нового детектора
Редактируйте `image_alignment.py`:
```python
def _init_detector(self):
    if self.feature_detector == 'your_detector':
        return YourDetector()
```

### Добавление новой визуализации
Редактируйте `visualizer.py`:
```python
@staticmethod
def create_your_visualization(img1, img2):
    # Ваш код
    return result
```

### Изменение формата вывода
Редактируйте `batch_processor.py` метод `process_pair()`.

---

**Последнее обновление:** 2025-01-15  
**Версия:** 1.0.0