# 🎯 Система автоматического совмещения изображений с двух камер

Автоматическое определение геометрических преобразований между изображениями, снятыми двумя одинаковыми камерами. Система вычисляет 8-параметрическую гомографию с точностью до пикселя.

## 📋 Описание

Проект предназначен для высокоточного совмещения изображений с двух близко расположенных камер. Основное применение — определение положения мелких частиц по разнице между совмещенными изображениями.

### Основные возможности

- ✅ Автоматическое обнаружение и сопоставление ключевых точек (ORB/SIFT/AKAZE)
- ✅ Вычисление полной гомографии (поворот, сдвиг, масштаб, перспектива)
- ✅ Декомпозиция преобразования на отдельные компоненты
- ✅ Точность совмещения ≤ 1 пиксель
- ✅ Пакетная обработка множества пар изображений
- ✅ Визуализация результатов (до/после, heatmap, overlay)
- ✅ Детальное логирование и JSON-отчеты

## 🚀 Установка

### Требования

- Python 3.10+
- Операционная система: Windows, Linux, macOS

### Установка зависимостей

```bash
# Клонируйте репозиторий
git clone <repository-url>
cd image-alignment

# Создайте виртуальное окружение (рекомендуется)
python -m venv venv

# Активируйте окружение
# Windows:
venv\Scripts\activate
# Linux/Mac:
source venv/bin/activate

# Установите зависимости
pip install -r requirements.txt
```

## 📁 Структура проекта

```
image-alignment/
├── main.py                    # Точка входа, CLI интерфейс
├── image_alignment.py         # Основной алгоритм совмещения
├── homography_decomposer.py   # Декомпозиция гомографии
├── batch_processor.py         # Пакетная обработка
├── visualizer.py              # Создание визуализаций
├── requirements.txt           # Зависимости
└── README.md                  # Документация
```

## 🎯 Использование

### Базовый запуск

```bash
python main.py --input ./images --output ./results
```

### Расширенные параметры

```bash
# Использование SIFT детектора с визуализациями
python main.py -i ./images -o ./results --detector sift --viz

# Отключить визуализации (только JSON)
python main.py -i ./images -o ./results --no-viz

# Детальное логирование в файл
python main.py -i ./images -o ./results -v --log processing.log

# Настройка параметров алгоритма
python main.py -i ./images -o ./results \
    --max-features 8000 \
    --ransac-threshold 2.5
```

### Параметры командной строки

| Параметр | Описание | По умолчанию |
|----------|----------|--------------|
| `-i, --input` | Директория с входными BMP изображениями | **обязательно** |
| `-o, --output` | Директория для результатов | **обязательно** |
| `--detector` | Тип детектора: `orb`, `sift`, `akaze` | `orb` |
| `--viz` | Создавать визуализации | `True` |
| `--no-viz` | Не создавать визуализации | - |
| `-v, --verbose` | Детальное логирование | `False` |
| `--log` | Путь к файлу лога | - |
| `--max-features` | Максимум ключевых точек | `5000` |
| `--ransac-threshold` | Порог RANSAC (пиксели) | `3.0` |

## 📊 Формат входных данных

### Схема нумерации файлов

- **Нечётные номера** (7, 9, 11, ...) → Эталонные изображения (камера 1)
- **Чётные номера** (8, 10, 12, ...) → Изображения для совмещения (камера 2)

Формат имени файла: `image<NUM>_b.bmp`

### Пример структуры входной директории

```
images/
├── image7_b.bmp   # Эталон
├── image8_b.bmp   # Совмещаемое
├── image9_b.bmp   # Эталон
├── image10_b.bmp  # Совмещаемое
└── ...
```

## 📤 Выходные данные

### Структура выходной директории

```
results/
├── image8_b_transform.json      # JSON с параметрами преобразования
├── image10_b_transform.json
├── summary_report.json          # Сводный отчет по всем парам
└── visualizations/              # Визуализации (если включены)
    ├── image8_b_before_after.jpg
    ├── image8_b_heatmap.jpg
    ├── image8_b_overlay.jpg
    ├── image8_b_matches.jpg
    ├── image8_b_checkerboard.jpg
    └── ...
```

### Формат JSON результата

```json
{
  "reference_file": "image7_b.bmp",
  "target_file": "image8_b.bmp",
  "processing_time_sec": 2.145,
  
  "homography": [
    [0.9987, -0.0123, 12.45],
    [0.0145,  0.9985, -5.32],
    [0.0001, -0.0002,  1.000]
  ],
  
  "transform_decomposed": {
    "rotation_deg": -0.85,
    "scale_x": 0.9992,
    "scale_y": 1.0008,
    "shift_x_px": 12.45,
    "shift_y_px": -5.32,
    "shear": 0.12,
    "perspective_x": 0.0001,
    "perspective_y": -0.0002,
    "perspective": [0.0001, -0.0002],
    "is_affine": true,
    "anisotropy": 0.9984,
    "condition_number": 1.0023
  },
  
  "quality": {
    "keypoints_reference": 4832,
    "keypoints_target": 4719,
    "matched_points": 2156,
    "inliers": 1987,
    "inlier_ratio": 0.9216,
    "rmse_pixels": 0.734
  }
}
```

## 🔍 Алгоритм работы

### 1. Детекция ключевых точек

- Используется ORB (по умолчанию), SIFT или AKAZE
- Предварительное улучшение контраста через CLAHE
- Обнаружение до 5000 ключевых точек на изображение

### 2. Сопоставление особенностей

- KNN matching с k=2
- Lowe's ratio test (порог 0.75)
- Фильтрация плохих совпадений

### 3. Вычисление гомографии

- RANSAC для устойчивости к выбросам
- Порог: 3 пикселя (настраивается)
- Уверенность: 99.5%
- До 5000 итераций

### 4. Декомпозиция преобразования

- SVD разложение для извлечения rotation и scale
- Извлечение translation из третьего столбца
- Анализ перспективных компонент

### 5. Оценка качества

- RMSE на inliers
- Inlier ratio
- Дополнительные метрики (анизотропия, число обусловленности)

## 📈 Визуализации

При включенном флаге `--viz` создаются следующие визуализации:

1. **Before/After** - Сравнение до и после совмещения
2. **Heatmap** - Тепловая карта разницы между изображениями
3. **Overlay** - Наложение с прозрачностью (красный/зеленый)
4. **Matches** - Сопоставленные ключевые точки
5. **Checkerboard** - Шахматное наложение для точной проверки

## 🎓 Технические детали

### Матрица гомографии

8-параметрическая гомография представляет собой проективное преобразование:

```
[x']   [h11  h12  h13] [x]
[y'] = [h21  h22  h23] [y]
[w']   [h31  h32   1 ] [1]
```

Финальные координаты: `x' = x'/w'`, `y' = y'/w'`

### Декомпозиция

Гомография раскладывается на:
- **Rotation** - поворот (градусы)
- **Scale** - масштаб по X и Y
- **Translation** - сдвиг в пикселях
- **Perspective** - перспективные искажения (эффект трапеции)
- **Shear** - наклон/сдвиг

## 🔧 Настройка под специфические задачи

### Высококонтрастные изображения

```python
# В image_alignment.py, метод detect_and_compute()
clahe = cv2.createCLAHE(clipLimit=3.0, tileGridSize=(8, 8))
```

### Изображения с линейкой

Линейка автоматически используется как ориентир благодаря feature matching. Для дополнительного учета можно добавить детекцию контуров:

```python
# Дополнительная фильтрация по геометрии линейки
edges = cv2.Canny(image, 50, 150)
lines = cv2.HoughLinesP(edges, ...)
```

### Низкое качество совпадений

Если `inlier_ratio < 0.5`, попробуйте:

1. Изменить детектор: `--detector sift`
2. Увеличить количество точек: `--max-features 8000`
3. Уменьшить порог RANSAC: `--ransac-threshold 2.0`

## 📝 Логирование

### Формат лога

```
2025-01-15 14:23:45 - INFO - Найдена пара: image7_b.bmp <-> image8_b.bmp
2025-01-15 14:23:45 - INFO - Размер изображений: (2048, 2048, 3)
2025-01-15 14:23:46 - INFO - Найдено 4832 ключевых точек
2025-01-15 14:23:46 - INFO - Найдено 2156 хороших совпадений из 4832
2025-01-15 14:23:46 - INFO - Inliers: 1987/2156 (92.16%)
2025-01-15 14:23:46 - INFO - RMSE: 0.734 пикселей
2025-01-15 14:23:47 - INFO - Декомпозиция: rotation=-0.85°, scale=(0.9992, 1.0008), shift=(12.5, -5.3)
```

### Сохранение лога в файл

```bash
python main.py -i ./images -o ./results --log processing.log
```

## 🧪 Тестирование

### Проверка точности

1. Запустите обработку:
```bash
python main.py -i ./test_images -o ./test_results --viz
```

2. Проверьте `summary_report.json`:
```json
{
  "average_rmse": 0.847,
  "average_inlier_ratio": 0.89
}
```

3. Визуально оцените качество по `checkerboard` и `overlay` изображениям

### Целевая точность

- **RMSE ≤ 1.0 px** - отличная точность ✅
- **RMSE ≤ 2.0 px** - хорошая точность ✓
- **RMSE > 2.0 px** - требуется оптимизация ⚠️

## 🐛 Troubleshooting

### Проблема: "Недостаточно совпадений"

**Решение:**
- Попробуйте другой детектор: `--detector sift`
- Проверьте качество исходных изображений
- Увеличьте `--max-features`

### Проблема: "Высокая RMSE (> 2 px)"

**Решение:**
- Уменьшите `--ransac-threshold`
- Проверьте, что изображения действительно одной сцены
- Убедитесь в отсутствии сильных искажений

### Проблема: SIFT недоступен

**Решение:**
```bash
pip install opencv-contrib-python
```

## 📚 Дополнительные ресурсы

- [OpenCV Feature Matching](https://docs.opencv.org/master/dc/dc3/tutorial_py_matcher.html)
- [Homography Estimation](https://docs.opencv.org/master/d9/dab/tutorial_homography.html)
- [RANSAC Algorithm](https://en.wikipedia.org/wiki/Random_sample_consensus)

## 📄 Лицензия

MIT License

## 👥 Контакты

Для вопросов и предложений создавайте Issues в репозитории.